/**
 * Shelf labels for the display forest. Settling itself is gone from this
 * add-on; the type remains so the renderer can keep its active/settled split
 * shape without inventing a second vocabulary.
 */

export type ThreadShelf = "active" | "settled";
